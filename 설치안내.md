# 사건관리 앱, 내 것으로 만들기 (변호사용 설치 안내)

이 문서 하나로 **나만의 사건관리 앱**을 만들 수 있습니다. 완성되면:

- 내 주소가 생깁니다: `https://<내 GitHub 아이디>.github.io/case-manager/`
- 모든 사건 데이터는 **내 구글 계정에 연결된 내 Firebase(구글 클라우드)** 에만 저장됩니다. 원본 제작자를 포함해 다른 누구도 볼 수 없습니다.
- 비용은 **0원**입니다 (GitHub 무료 + Firebase 무료 등급, 개인 사용 규모로 충분).

## 준비물

1. **구글 계정** (업무용으로 쓸 계정)
2. **GitHub 계정** — 없으면 https://github.com/signup 에서 무료 가입 (아이디는 앱 주소에 들어가니 신중히)
3. **클로드 코드(Claude Code)** — 아래 과정을 클로드가 대신 실행해 줍니다

## 시작 방법 (사용자가 할 일)

클로드 코드를 열고, 빈 작업 폴더에서 이렇게 말하세요:

> 이 안내대로 내 사건관리 앱을 세팅해줘:
> https://github.com/kcozy11-prog/case-manager/blob/main/설치안내.md

이후 클로드가 단계마다 안내합니다. 사용자가 직접 하는 일은 **브라우저에서 로그인·승인 몇 번**뿐입니다:

- GitHub 기기 인증 (클로드가 알려주는 일회용 코드 입력)
- Firebase 콘솔에서 프로젝트 만들기 (클로드가 화면 순서대로 안내)
- 완성된 앱에 구글 로그인

⚠️ 비밀번호·API 키를 클로드 채팅에 붙여넣지 마세요. 로그인은 항상 브라우저에서 직접 합니다. (아래 6단계의 Firebase 웹 구성값은 비밀정보가 아니므로 붙여넣어도 됩니다.)

---

## 클로드 실행 지시서 (Claude가 읽고 수행하는 부분)

당신(Claude)은 아래 순서로 사용자의 사건관리 앱을 세팅한다. 각 단계의 검증을 통과한 뒤 다음으로 넘어간다. 사용자는 비개발자 변호사이므로 브라우저에서 할 일은 화면 단위로 짧게 안내한다.

### 1. 도구 준비
- `git`, GitHub CLI(`gh`) 설치 여부 확인. 없으면 설치한다 (Windows: `winget install GitHub.cli`).
- `gh auth status`가 미인증이면 `gh auth login --hostname github.com --git-protocol https --web`을 실행하고, 출력된 **일회용 코드**와 `https://github.com/login/device` 주소를 사용자에게 안내한다. 인증 완료를 확인한다.
- `gh auth setup-git` 실행.

### 2. 템플릿에서 내 저장소 만들기
```
gh repo create case-manager --template kcozy11-prog/case-manager --public --clone
```
- 저장소 이름은 반드시 `case-manager` 그대로 둘 것 (앱 내부 경로가 이 이름에 맞춰져 있음).
- 무료 GitHub Pages는 공개(Public) 저장소에서만 동작하므로 Public으로 만든다. 사건 데이터는 저장소가 아니라 Firebase에 저장되므로 공개되어도 데이터는 노출되지 않는다.
- 검증: `gh repo view <아이디>/case-manager` 성공.

### 3. Firebase 프로젝트 생성 (사용자 브라우저 작업, 화면별 안내)
사용자에게 https://console.firebase.google.com 접속을 안내하고 순서대로:
1. **프로젝트 추가** → 이름 예: `case-manager` → 애널리틱스는 꺼도 됨 → 만들기
2. 왼쪽 **빌드 > Authentication** → 시작하기 → **Sign-in method** 탭 → **Google** 사용 설정(프로젝트 지원 이메일 선택) → 저장
3. 왼쪽 **빌드 > Firestore Database** → 데이터베이스 만들기 → 위치 `asia-northeast3`(서울) → **프로덕션 모드** → 만들기
4. Firestore **규칙** 탭에 저장소의 `firestore.rules` 파일 내용을 그대로 붙여넣고 **게시** (Claude가 파일 내용을 보여준다)

### 4. 웹 앱 등록 및 구성값 받기 (사용자 브라우저 작업)
1. Firebase 콘솔 좌측 상단 **⚙️ 프로젝트 설정 > 일반** → 아래 **내 앱** → 웹 아이콘 **`</>`** 클릭
2. 앱 닉네임 아무거나 → 앱 등록 (호스팅 체크 불필요)
3. 화면에 나오는 `firebaseConfig` 코드 블록(apiKey, authDomain, projectId, storageBucket, messagingSenderId, appId 6개 값)을 **복사해서 클로드에게 붙여넣기** — 이 값들은 비밀정보가 아니라 안전하다.

### 5. 저장소 변수 설정 (Claude)
받은 6개 값으로:
```
gh variable set VITE_FIREBASE_API_KEY --body "<apiKey>"
gh variable set VITE_FIREBASE_AUTH_DOMAIN --body "<authDomain>"
gh variable set VITE_FIREBASE_PROJECT_ID --body "<projectId>"
gh variable set VITE_FIREBASE_STORAGE_BUCKET --body "<storageBucket>"
gh variable set VITE_FIREBASE_MESSAGING_SENDER_ID --body "<messagingSenderId>"
gh variable set VITE_FIREBASE_APP_ID --body "<appId>"
```
- 검증: `gh variable list`에 6개 모두 표시.

### 6. 빌드·배포 실행 (Claude)
```
gh workflow run deploy.yml
```
- `gh run watch` 또는 `gh run list`로 성공(✓) 확인. 실패 시 로그를 읽고 원인(대부분 변수 오타)을 고쳐 재실행.
- 검증: `https://<아이디>.github.io/case-manager/` 가 HTTP 200으로 응답.

### 7. 로그인 도메인 승인 (사용자 브라우저 작업)
Firebase 콘솔 → **Authentication > Settings(설정) > 승인된 도메인** → **도메인 추가** → `<아이디>.github.io` 입력 → 추가.
(이 단계를 빼먹으면 앱에서 구글 로그인이 거부된다.)

### 8. 최종 확인 (사용자)
1. `https://<아이디>.github.io/case-manager/` 접속 → **구글 로그인**
2. "확인되지 않은 앱" 경고가 나오면: **고급 → 이동(계속)** — 방금 본인이 만든 프로젝트라 안전하다.
3. 캘린더 권한 동의에서 계속 막히면: Google Cloud Console(console.cloud.google.com) → 해당 프로젝트 → **API 및 서비스 > OAuth 동의 화면 > 테스트 사용자**에 본인 이메일 추가 후 재로그인.
4. 사건 하나를 등록해 보고, 다른 기기(폰)에서 같은 주소로 로그인해 동기화되는지 확인.

### 참고 (Claude용)
- 이후 앱 수정 요청이 오면: 이 폴더에서 코드를 고치고 main에 push하면 자동 배포된다.
- 구글 캘린더/스프레드시트 연동은 앱에서 로그인할 때 권한 동의로 처리되며 별도 설정이 없다.
- 원본 저장소가 업데이트되었을 때 반영하려면: `git remote add upstream https://github.com/kcozy11-prog/case-manager.git && git fetch upstream && git merge upstream/main` 후 충돌을 해결하고 push.

---

## 자주 묻는 것

| 질문 | 답 |
|---|---|
| 진짜 무료인가? | GitHub·Firebase 모두 무료 등급으로 충분합니다. 결제 등록도 필요 없습니다. |
| 내 사건 기록을 원본 제작자가 볼 수 있나? | 아니요. 데이터는 본인 Firebase에만 저장되고, 앱 규칙상 본인 계정만 읽고 쓸 수 있습니다. |
| 폰에서도 되나? | 네. 폰 브라우저로 접속해 "홈 화면에 추가"하면 앱처럼 쓸 수 있습니다. |
| 데이터 백업은? | 앱의 내보내기 기능을 이용하거나, Firebase 콘솔에서 언제든 데이터를 확인할 수 있습니다. |
